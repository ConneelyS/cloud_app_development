<%= form_with(model: doctor_note, local: true) do |form| %>
  <% if doctor_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctor_note.errors.count, "error") %> prohibited this doctor_note from being saved:</h2>

      <ul>
      <% doctor_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.number_field :patient_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
